events {}

http {
    include                 /etc/nginx/mime.types;

    # Proxy API requests
    server {
        listen              80;
        server_name         api.sophia-dukhota.com;
        location / {
            proxy_pass http://booksapi;
        }
    }

    # Redirect www HTTP to root HTTP
    server {
        listen              80;
        server_name         www.sophia-dukhota.com;
        location / {
            return 301 https://sophia-dukhota.com$request_uri;
        }
    }

    # Main root domain HTTPS block
    server {
        listen              80;
        server_name         sophia-dukhota.com;
        root                /var/www/html;
        index               index.html;
        location / {
            try_files       $uri $uri/ /index.html;
        }
        location /app {
            try_files       $uri /app/index.html;
        }
    }
}